<section class="bg-white dark:bg-black min-h-screen flex items-center justify-center px-6 relative overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
        <!-- Grid pattern -->
        <div class="absolute inset-0 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear-gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:24px_24px]"></div>
        
        <!-- Grid fade overlay -->
        <div class="absolute inset-0" style="background: radial-gradient(ellipse 70% 60% at center, rgba(255,255,255,0.8), rgba(255,255,255,0.3) 60%, transparent 80%);"></div>
        <div class="absolute inset-0 dark:block hidden" style="background: radial-gradient(ellipse 70% 60% at center, rgba(0,0,0,0.6), rgba(0,0,0,0.3) 60%, transparent 80%);"></div>
    </div>

    <div class="max-w-6xl mx-auto text-center relative z-10">
        <!-- Badge -->
        <div class="inline-flex items-center gap-2 px-4 py-2 bg-sky-500/10 border border-sky-500/20 rounded-full text-sm font-medium text-sky-600 dark:text-sky-400 mb-8 backdrop-blur-sm">
            <div class="w-2 h-2 bg-sky-500 rounded-full animate-pulse"></div>
            Freelance Web Developer
        </div>

        <!-- Main headline -->
        <h1 class="text-5xl sm:text-6xl md:text-7xl lg:text-8xl font-black text-black dark:text-white mb-6 leading-[0.9] tracking-tight">
            <span class="block">Il tuo nuovo sito.</span>
            <span class="block bg-gradient-to-r from-sky-500 to-sky-600 bg-clip-text text-transparent">
                Fatto come si deve.
            </span>
        </h1>

        <!-- Subheadline -->
        <p class="text-lg md:text-xl text-gray-600 dark:text-gray-300 mb-12 max-w-2xl mx-auto leading-relaxed font-normal">
            Progetto e sviluppo siti web curati, veloci e su misura per dare valore alla tua attività online.
        </p>

        <!-- CTA Button -->
        <div class="mb-16">
            <a href="#" class="group bg-sky-500 text-white px-8 py-4 text-base font-semibold rounded-full tracking-wide relative inline-flex items-center gap-2 border border-sky-600 hover:bg-sky-600 hover:border-sky-700 transition-all duration-300">
                Inizia Ora
            </a>
        </div>
    </div>

    <!-- Scroll indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
        <svg class="w-6 h-6 text-gray-400 dark:text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
    </div>
</section>