<section class="bg-white dark:bg-black min-h-screen flex items-center justify-center px-6 py-12 relative">
    <div class="max-w-7xl mx-auto text-center relative z-10">
        <!-- Badge -->
        <div class="inline-flex items-center gap-2 px-4 py-2 bg-sky-500/10 border border-sky-500/20 rounded-full text-sm font-medium text-sky-600 dark:text-sky-400 mb-8 backdrop-blur-sm">
            <div class="w-2 h-2 bg-sky-500 rounded-full animate-pulse"></div>
            Freelance Web Developer
        </div>

        <!-- Hero Title -->
        <h1 class="text-6xl sm:text-7xl md:text-8xl lg:text-9xl xl:text-[10rem] font-black mb-8 tracking-normal">
            <span class="block leading-[0.95] md:leading-[0.75] bg-gradient-to-r from-sky-400 via-sky-500 to-sky-600 bg-clip-text text-transparent">
                No website?
            </span>
            <span class="block leading-[0.95] bg-gradient-to-r from-sky-400 via-sky-500 to-sky-600 bg-clip-text text-transparent">
                No party.
            </span>
        </h1>

        <!-- Hero Description -->
        <p class="text-base md:text-lg text-gray-500 dark:text-gray-400 mb-12 max-w-sm md:max-w-xl mx-auto leading-normal tracking-tight font-normal opacity-80">
            Essere online non basta: serve esserci nel modo giusto.  Scopri come posso creare o rinnovare il sito che meriti.
        </p>

        <!-- CTA Button -->
        <div class="mb-16">
            <a href="#" class="inline-flex items-center gap-2 bg-sky-500 hover:bg-sky-600 text-white dark:text px-8 py-4 text-base font-semibold rounded-full transition-colors duration-200">
                Inizia Ora
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                </svg>
            </a>
        </div>
    </div>

    <!-- Scroll indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
        <svg class="w-6 h-6 text-gray-400 dark:text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
    </div>
</section>