<div id="loader" class="loader-container">
    <div class="logo-container">
        <img src="/favicon.svg" alt="Logo" class="logo" />
    </div>
</div>

<style>
.loader-container {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: #ffffff;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 9999;
    transition: opacity 0.5s ease-in-out, visibility 0.5s;
    overflow: hidden;
}

:global(.dark) .loader-container {
    background-color: #111827; /* Matches dark:bg-gray-900 */
}

:global(body.no-scroll) {
    overflow: hidden;
    height: 100%;
    margin: 0;
    padding: 0;
}

.logo-container {
    animation: zoom 1.5s infinite ease-in-out;
}

.logo {
    width: 120px;
    height: 120px;
}

@keyframes zoom {
    0% {
    transform: scale(0.9);
    opacity: 0.8;
    }
    50% {
    transform: scale(1.1);
    opacity: 1;
    }
    100% {
    transform: scale(0.9);
    opacity: 0.8;
    }
}

.loader-container.hidden {
    opacity: 0;
    visibility: hidden;
}
</style>