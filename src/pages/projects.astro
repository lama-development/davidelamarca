---
import Layout from "../layouts/Layout.astro";
import ProjectsGrid from "../components/projects/Projects.astro";
import language from "../i18n/it";
const t = language();
---

<Layout t={t} meta={t.projects.meta}>
  <ProjectsGrid header={t.projects.header} projectsList={t.projects.projectsList} isHomePage={false} />
</Layout>
