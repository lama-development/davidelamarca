---
import Layout from "../layouts/Layout.astro";
import Header from "../components/common/Header.astro";
import TermsOfService from "../components/legal/TermsOfService.astro";
import language from "../i18n/it";
const t = language();
---

<Layout t={t} meta={t.termsOfService.meta}>
  <Header header={t.termsOfService.header} />
  <TermsOfService terms={t.termsOfService} />
</Layout>
